#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main()
{
    int ch;
    int sch;

    for (ch = 65; ch < 91; ch++)
    {
        putchar(ch);
    }
    for (sch = 97; sch < 123; sch++)
    {
        putchar(sch);
    }
    
    
}
